export default interface ITheme {
    strain: {
        background: string;

        border: string;
        dock: string;
        borderRadius: number;
    };
    flow: {
        background: string;
        border: string;
        step: number;
    };
}
